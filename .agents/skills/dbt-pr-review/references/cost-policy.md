# Cost policy

Partitioned BigQuery-compatible SQL requires a bounded partition predicate. Phase 8A estimates are
simulated fixtures only. Missing Sandbox or Cloud evidence is unavailable, not zero and not failure.
