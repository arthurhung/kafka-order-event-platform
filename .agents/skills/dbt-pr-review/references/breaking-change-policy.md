# Breaking-change policy

Published column removal, incompatible type change, grain/key change, requiredness tightening, model
removal, and contract removal are blocking. Missing baseline is degraded, never a pass.
