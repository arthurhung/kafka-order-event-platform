# Remediation boundaries

Only query, inspect, correlate, analyze, summarize, recommend, and generate review-only remediation,
backfill, and validation plans. Never mutate data/schema/IAM, rerun, reset offsets, deploy, merge,
push, commit, execute arbitrary SQL/shell, or acknowledge an incident for a human.
