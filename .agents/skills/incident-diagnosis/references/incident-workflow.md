# Incident workflow

Validate, resolve the asset, inspect owner/SLO, quality/freshness, upstream/downstream lineage,
pipeline failures, Kafka lag, and cost evidence. Correlate a timeline and stop at human review.
